DRINKS PAGE + HEADER FIX — overwrite into scratchkitchenbygallosv2
==================================================================
HTML paths use "next/" (no underscore).

drinks.html now lists the real beer & wine menu (from the Toast B1 page)
in three anchored sections with a sticky category nav:
  - Drafts (10)
  - Red Wine (6)          by the glass
  - White, Rose & Prosecco (11, the two duplicate Toast categories merged)
"(Copy)" is omitted; there is NO external Toast link button.

Header (all pages): every nav tab stays on ONE line (no wrapping).

Assets: assets/scratch-kitchen-drinks.mp4 (hero video),
img/beer-mug.webp, img/wine-red.webp, img/wine-selection.webp.

APPLY: copy everything into the repo root, overwriting. Keep .nojekyll at root.
